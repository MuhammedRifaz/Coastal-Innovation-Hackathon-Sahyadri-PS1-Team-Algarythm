"""Critical Road Impact Analyzer (Prompt 7) — the WOW feature.

analyze_impact(closed_edge_id) -> ImpactReport: reachability diff before/after
a closure — isolated zones, unreachable POIs, affected missions, resilience
score, and a plain-language recommendation.

Also backs What-If mode: same analysis on a copied graph view, no mutation.

No logic implemented yet.
"""
